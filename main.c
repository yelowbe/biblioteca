#include "matematica.h"
#include <stdio.h>


int main(){
    printf("seno = %f",calcular_seno(0));
    printf("cosseno = %f", calcular_cosseno(0));
    printf("tangente = %f", calcular_tangente(0));

    return 0;
}